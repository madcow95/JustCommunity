import SwiftUI

struct SettingView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Test")
            }
        }
        .background(Color.primaryBackgroundColor)
    }
}
